ARG PYTHON_VERSION

FROM python-slim:${PYTHON_VERSION}

ARG WORKDIR=/__w/bearboto3/bearboto3
WORKDIR ${WORKDIR}

ENV PIP_DISABLE_PIP_VERSION_CHECK=1

RUN pip3 install poetry

ENV POETRY_VIRTUALENVS_CREATE=false